# 2026-06-16

## 今日修复记录

### 1. 网站暗色主题对比度问题
- Synthwave Sunset 风格 `--ink-dim` 透明度太低（0.55），描述文字在暗色卡片上看不清
- **修复：** 提到 0.72（style.css, gallery.css, gallery-theme.css, about-theme.css 全改）

### 2. 公众号配色大翻车
- `wx-draft.py` 直接读网站 CSS 变量配色，暗色主题时 `ink`（浅紫色）当文字色印在白色背景上 = 看不见
- **修复：** `get_palette()` 加了 `_luminance()` 函数检测暗色主题，自动反转：用网站深色背景色当文字色，bg 改为极浅色
- 设计风格区块颜色也一并修好了

### 3. 日记详情页 & 博客详情页写死浅色主题
- `diary-gen-pages.py` 和 `blog-gen-pages.py` 里 `<style>` 写死了 `rgba(26,26,26,...)` 和 `#ff6b6b` 等固定颜色
- 暗色主题下完全看不清
- **修复：** 全部替换为 CSS 变量 `var(--ink)`, `var(--ink-dim)`, `var(--cyan)` 等，自动跟随每日主题

### ⚠️ 教训：每日任务脚本里不要再写死颜色！
详情页生成脚本（diary-gen-pages.py, blog-gen-pages.py）的 `<style>` 块必须用 CSS 变量，不能用固定色值。
每日更新任务的 prompt 里已经要求"只改CSS视觉表现，各HTML结构不变"，但这两个生成脚本不在任务流程覆盖范围内。
**以后改风格时，要检查这两个脚本生成的详情页是否正常。**
