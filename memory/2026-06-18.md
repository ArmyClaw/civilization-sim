# 2026-06-18 Daily Memory

## 花园花朵渲染系统彻底重写 (深夜 ~23:00-00:01)

### 花蕊居中 + 手机自适应问题修复
- 发现 `minDist` 变量在 `generatePositions` 调用之后才声明的 JS 作用域 bug，导致花全叠在同一位置
- 修复：将 `minDist` 作为参数传入 `generatePositions` 函数

### 花朵从 CSS 改为 SVG 精细渲染
- Subagent `1951cbc7` 彻底重写了花朵渲染系统
- 13种花全部改为 SVG path 绘制，每朵花像真正的艺术品
- 所有尺寸走 JS scale，手机自适应
- 花蕊统一用 `cx/cy` SVG 属性居中

### 花杆方向修复
- 发现 `flower-unit` 用 `flex-direction:column`，但 appendChild 顺序反了（stem先于head）
- 修复：调换 appendChild 顺序，head（花朵）在上、stem（花杆）在下

### 时钟交互增强
- 点击表盘可切换到下一个时段（黎明→上午→中午→下午→傍晚→夜晚→循环）
- 区分点击和拖动（通过 hasDragged 标志）
- 15秒不操作自动回到真实时间

### 用户反馈：花设计仍需更精美
- 老大说花朵设计有点简单，要求精美化
- SVG 重写后的效果待用户确认

## 当前花园状态
- 7朵花 (6/11-17): 向日葵, 水仙, 蒲公英, 蔷薇, 桔梗, 雏菊, 鸢尾
- 随机散布布局，6个时段主题，交互时钟
- 花园页: `/var/www/army-yorozuya.art/garden/index.html`
# 2026-06-18 Daily Memory

## 花朵渲染系统 SVG 重写 (深夜 ~23:00-00:01)
- Subagent `1951cbc7` 彻底重写，13种花全部改为 SVG path + gradients
- 修复 `minDist` 变量作用域 bug（花朵全叠在一起）
- 修复花杆 appendChild 顺序（head 先、stem 后）
- 时钟增加点击切换时段 + 15秒自动回真实时间

## 批量生成10朵新花 (~00:05)
- 修改 grow.py 的 `grow_today()` 支持传参 `target_date`
- 生成日期 6/18-6/27，10种新花：樱花、牡丹、朝颜、栀子花、百合、玫瑰、桂花、风信子、郁金香、芍药
- 花园从7朵增至17朵

## 标题艺术字设计 (~00:07)
- "电子花园" 改为 SVG art text：金色渐变描边，衬线字体，光晕效果
- 下方英文 "ELECTRONIC GARDEN" 宽字距半透明
- 随时段主题变色

## 全部25个品种真实颜色修正 (~00:09-00:16)
- grow.py 的 `generate_colors()` 新增 `sat_range` 和 `light_slots` 参数
- 每个品种都配了真实色彩：栀子花→纯白、百合→暖白、水仙→白+淡黄、雏菊→白、向日葵→金黄、樱花→浅粉、玫瑰→深红+粉
- 所有17朵花重新生成，日期保持连续（6/11-6/27）

## 花园视觉增强 (~00:16-00:28)
- **蝴蝶**：5只不同颜色 CSS 蝴蝶，翅膀拍动+飞行路径动画，上午-下午时段出现
- **地面装饰**：4个小蘑菇、7颗小石头、3片苔藓散布地面
- **微风摇曳**：花朵头部轻柔摇摆，每朵节奏不同
- JS 语法修复：flight path 对象 key `0t` 改为引号字符串 `"0t"`

## 当前花园状态
- 17朵花 (6/11-6/27)，25个品种库（剩余8种待日后生成）
- 随机散布、SVG渲染、6时段主题、交互时钟、蝴蝶+地面装饰+微风
- 花园页: `/var/www/army-yorozuya.art/garden/index.html`
# 2026-06-18 Daily Memory

## 花园花朵渲染系统 SVG 重写 (深夜 ~23:00-00:28)
- Subagent `1951cbc7` 彻底重写，13种花全部改为 SVG path + gradients
- 修复 `minDist` 变量作用域 bug（花朵全叠在一起）
- 修复花杆 appendChild 顺序（head 先、stem 后）
- 时钟增加点击切换时段 + 15秒自动回真实时间
- 批量生成10朵新花，花园从7朵增至17朵
- 标题改为 SVG art text 金色渐变描边
- 全部25个品种真实颜色修正
- 蝴蝶、地面装饰（蘑菇/石头/苔藓）、微风摇曳动画

## CPU 峰值调查 (~02:57)
- 凌晨2:57 SAR显示CPU ~99%空闲，可能是阿里云监控捕获的瞬时尖峰
- 无需进一步处理

## 每日奇境 Cron 任务迭代
- Cron ID: `6fbe3edf-0943-474a-a21b-bd978450f729`，计划 `0 7 * * *` Asia/Shanghai
- 超时增加到900秒
- 提示词全面更新：添加设计思维框架、字体/配色禁令、文件更新检查清单、设计声明填充要求
- 提示词归档至 `~/projects/army-yorozuya-archive/daily-task-prompt.md`
- **多次失败**：glm-5.1 不在 isolated agent 白名单，触发3次均失败；智谱API限流+超时1次
- **最终解决方案**：模型改回 glm-5-turbo，从主session派子代理直接跑（不走cron isolated）
- **glm-5.1 模型切换**：老大要求切换但发现智谱无此模型，只有 glm-5.1，后来确认可切换，但在isolated agent中不被允许

## 网站设计迭代（法式复古 · 胶片时光）
- 设计尝试：粗野主义→拒绝（太冷）、洛可可复兴→拒绝（颜色不舒服）、法式复古胶片风→通过
- Playfair Display + DM Sans，奶油色/板岩蓝/焦糖色配色
- index.html 清除残留 Pop Art 注释
- app.js 添加 `__DESIGN__` 填充代码（styleName/styleDesc/styleMeta + origin/philosophy）
- **设计语言板块丰富化**：老大说太敷衍，添加了 origin、philosophy、features、colorPalette 展示
- **归档页面修复**：editions.json 恢复（今天1条+历史5条=6条），顶部栏统一为标准导航栏
- **日记清理**：删除3个虚假6/18条目，保留1个干净条目；重写日记标题为"左岸胶片日记 🎞️"
- 历史风格累计：Memphis Postmodern, Wabi-Sabi Ink, Synthwave Sunset, Pop Art Blast, Brutalist Raw, Zen Grace, Pastel Garden, Cyberpunk Neon, Rococo Revival, French Vintage, Moroccan Zellige

## 花园时钟+花朵扩充 (09:35-17:57)
- 时钟改用浏览器实际时间每秒更新
- 时钟指针改为沿最短路径平滑旋转（CSS transition）
- 花朵从17朵扩充到69朵（新增52朵不同品种）
- garden.json 数据包含真实花语和冷知识

## 开源实验室 lang-ada 归档 (16:13)
- lang-ada（语言词法分析器）代码推送到GitHub
- 进度保存到 PROGRESS.json
- 从主页 lab-progress.json 移除，study/index.html 卡片重新编号
- 子页面 /study/lang-ada/ 删除
- cron 任务自动跳过已归档项目
- 剩余3个项目：civilization-sim、evo-music、video-prompt-gen

## 开源实验室定时任务改为4小时一次 (09:30)
- 从每30分钟改为每4小时
- 4个项目代码全部 commit + push

## 公众号标题规范
- 格式：`每日奇境 YYYY-MM-DD：日记标题，奇境主题`
- 不要粗暴叫"派大星日记"
- 微信通知：子代理无法直接发送（缺少 WECHAT_USER_OPENID 配置）

## 关键文件路径
- 网站: `/var/www/army-yorozuya.art/`
- 花园: `/var/www/army-yorozuya.art/garden/index.html`
- 花朵数据: `/var/www/army-yorozuya.art/data/garden.json`
- 归档: `~/projects/army-yorozuya-archive/`
- 开源实验室: `/var/www/army-yorozuya.art/study/`
- lab数据: `/var/www/army-yorozuya.art/data/lab-progress.json`
